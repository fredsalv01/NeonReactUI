# NPM Configuration
# package-lock.json is ignored and regenerated per environment
# This prevents merge conflicts and version inconsistencies

save-exact=false
legacy-peer-deps=false

# Ensure clean installs use npm ci (CI workflow)
# Local development uses npm install
